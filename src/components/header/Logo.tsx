import * as S from "../../styles/header";

export default function Logo() {
  return (
    <S.Logo>
      <S.LogoIcon>
        <S.Bar className="left" />
        <S.Bar className="middle" />
        <S.Bar className="right" />
      </S.LogoIcon>
      kaban
    </S.Logo>
  );
}
